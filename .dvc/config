[core]
    remote = storage
['remote "storage"']
    url = s3://mlflow/dvc-storage
    endpointurl = http://localhost:9002
    use_ssl = false
    access_key_id = minio
    secret_access_key = minio12345
['remote "minio_src"']
    url = s3://mlflow
    endpointurl = http://localhost:9002
    use_ssl = false
    access_key_id = minio
    secret_access_key = minio12345
